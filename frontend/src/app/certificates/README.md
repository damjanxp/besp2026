# Certificates UI

This folder contains the certificate list view and details dialog used by the PKI UI.

- Route: `/certificates`
- Data source: `GET /api/certificates`
- Details dialog: `GET /api/certificates/{id}`
- Downloads: `GET /api/certificates/{id}/download?format=PEM|DER`

