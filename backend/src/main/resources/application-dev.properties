# H2 in-memory database for development (no PostgreSQL needed)
spring.datasource.url=jdbc:h2:mem:pki_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# H2 Console (accessible at http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Gmail SMTP
# Tvoj Gmail nalog sluzi kao POSILJALAC ? email stize na adresu kojom se bilo koji korisnik registruje.
# MAIL_USERNAME = tvoj Gmail (npr. moj.projekat@gmail.com)
# MAIL_PASSWORD = Gmail App Password (16-cifarski kod, NE prava lozinka)
#
# Kako napraviti App Password:
#   1. https://myaccount.google.com/security  ?  uklju?i 2-Step Verification
#   2. https://myaccount.google.com/apppasswords  ?  klikni Create  ?  npr. "PKI App"
#   3. Kopiraj 16-cifreni kod (bez razmaka) u MAIL_PASSWORD env varijablu
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=damjanpetrov53@gmail.com
spring.mail.password=ijgnoioefpmhwiil
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
